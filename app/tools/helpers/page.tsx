import Helpers from "../Helpers";

export const metadata = {
  title: "Array & Object Data Generator",
  description:
"Generate mock arrays, objects, and utility data online. FakerBox creates structured test data for development, APIs, and UI prototyping. Generate now."
                
};

export default function Page() {
  return <Helpers />;
}
