import Book from "../../tools/Book";

export const metadata = {
  title: "Fake Book Data Generator – Create Random Titles & Authors",
  description:
  "Generate realistic fake book data, including titles, authors, and library info for reading platform testing, demos, development, etc. Generate now."
};

export default function Page() {
  return <Book />;
}
