import Food from "../Food";

export const metadata = {
  title: "Random Recipe Generator – Create Fake Receipe Data",
  description:

  "Generate fake recipes, dishes & grocery lists in seconds with our free random recipe generator. Create now."
};

export default function Page() {
  return <Food />;
}
