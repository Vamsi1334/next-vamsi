import Image from "../Image";

export const metadata = {
  title: "Fake Image URL Generator | Fakerbox",
  description:
  "Generate random image URLs and mock photo data for development and design. Instantly get placeholder URLs for media, testing, and sample visual content."
        
};

export default function Page() {
  return <Image />;
}
