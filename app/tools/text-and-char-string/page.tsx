import String from "../String";

export const metadata = {
  title: "Random Text String Generator - Fakerbox",
  description:
"Create random text or 32-char sample strings, alpha, alphanumeric, UUID, NanoID. Flexible mock string data generator for development use. Generate now."
       
};

export default function Page() {
  return <String />;
}
