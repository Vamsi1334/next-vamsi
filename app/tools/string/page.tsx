import Random from "../Random";

export const metadata = {
  title: "Random String Generator – UUID, NanoID & More",
  description:
"Generate random strings, UUIDs, Nano IDs, symbols & more. Fast, customizable tool for developers to create mock identifiers & unique test data instantly."        
};

export default function Page() {
  return <Random />;
}
